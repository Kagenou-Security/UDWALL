<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.UDWALL" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Using the vector foreground icon for perfect quality -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenBackground">@color/black</item>

        <!-- Post-splash theme -->
        <item name="postSplashScreenTheme">@style/Theme.UDWALL</item>
    </style>
</resources>
